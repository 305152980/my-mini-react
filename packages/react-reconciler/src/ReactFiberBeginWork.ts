import { type Fiber } from './ReactInternalTypes'

// 1、处理当前 Fiber 节点。
// 2、返回子 Fiber 节点。
export function beginWork(
  current: Fiber | null,
  workInProgress: Fiber
): Fiber | null {
  // TODO
  return null
}
